# Locket — Quick Start

*Coming soon — written once the MVP exists.*

Planned flow:

1. Install the `locket` binary next to your PocketBase install
2. Point it at `/opt/pocketbase/projects.conf`
3. Open the dashboard → see your fleet
4. Deep-link into any instance's admin

*Status: not yet available (MVP not built).*
